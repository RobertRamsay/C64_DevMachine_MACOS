diff --git a/C64Devmachine/objects/obj_workspace_manager/Create_0.gml b/C64Devmachine/objects/obj_workspace_manager/Create_0.gml
index d93e630..1cf536a 100644
--- a/C64Devmachine/objects/obj_workspace_manager/Create_0.gml
+++ b/C64Devmachine/objects/obj_workspace_manager/Create_0.gml
@@ -614,6 +614,7 @@ qmenu_gui_x    = 0;
 qmenu_gui_y    = 0;
 qmenu_hover    = -1;
 qmenu_items    = [
+    { label: "COPY VAR", type: "COPY_VAR" },
     { label: "GET VAR", type: "GET_VAR" },
     { label: "SET VAR", type: "SET_VAR" },
     { label: "INC VAR", type: "INC_VAR" },
diff --git a/C64Devmachine/scripts/scr_qmenu_layout/scr_qmenu_layout.gml b/C64Devmachine/scripts/scr_qmenu_layout/scr_qmenu_layout.gml
index 3128833..5ddd47a 100644
--- a/C64Devmachine/scripts/scr_qmenu_layout/scr_qmenu_layout.gml
+++ b/C64Devmachine/scripts/scr_qmenu_layout/scr_qmenu_layout.gml
@@ -2,13 +2,16 @@ function scr_qmenu_layout(_index, _cx, _cy) {
     var _btn_w = 100;
     var _btn_h = 24;
 
-    var _row_y   = [-50, -50, 0, 0, 50, 50];
-    var _narrow_x = 60;
-    var _wide_x   = 110;
-    var _col_x   = [-_narrow_x, _narrow_x, -_wide_x, _wide_x, -_narrow_x, _narrow_x];
+    // Circular ring, sized off the item list itself so adding/removing a
+    // fixed entry here (e.g. COPY VAR) just reflows the ring automatically.
+    // Same radius growth as the Q custom menu's single-ring tier, so the
+    // two menus feel consistent at the same item count.
+    var _count  = array_length(qmenu_items);
+    var _radius = 150 + max(0, _count - 5) * 10;
+    var _angle  = 90 - (_index * (360 / _count)); // index 0 sits at the top (90°)
 
-    var _cx2 = _cx + _col_x[_index];
-    var _cy2 = _cy + _row_y[_index];
+    var _cx2 = _cx + lengthdir_x(_radius, _angle);
+    var _cy2 = _cy + lengthdir_y(_radius, _angle);
 
     return [_cx2 - (_btn_w / 2), _cy2 - (_btn_h / 2), _cx2 + (_btn_w / 2), _cy2 + (_btn_h / 2)];
 }
\ No newline at end of file
