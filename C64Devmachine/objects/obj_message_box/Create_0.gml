/// @desc Initialise modal info dialog state
message      = "";
dismissed    = false;

// Suppress the initial mouse click that may have spawned this dialog
input_armed  = false;

// Layout (GUI space)
box_w        = 540;
box_h        = 200;
btn_w        = 160;
btn_h        = 48;

// Hover tracking
hover_ok     = false;

// Pulse for visual interest
pulse        = 0;

depth        = -10000;