		keyboard_clear(vk_control);
        keyboard_clear(vk_shift);
        keyboard_clear(vk_alt);
        keyboard_clear(ord("Z")); // belt-and-braces for cmd remnants
		keyboard_clear(vk_anykey)
		keyboard_clear(160)
		keyboard_clear(162)
		keyboard_clear(92)
		keyboard_clear(91)
		
		

		
